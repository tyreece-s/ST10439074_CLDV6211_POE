@model IEnumerable<EventEase.Models.Venue>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>

@* Displaying the Venues with Cards instead of the basic table using Bootstrap *@

<div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
    @foreach (var item in Model)
    {
        @* Card *@
        <div class="col">
            <div class="card h-100 shadow-sm border-0 rounded-3">
                @* Image at the top *@
                <img src="@item.ImageUrl"
                     class="card-img-top rounded-top"
                     alt="Venue Image"
                     style="height: 250px; object-fit: cover;"/>

                @* Card Details for Venue *@
                <div class="card-body">
                    <h5 class="card-title mb-2"><strong>@item.VenueName</strong></h5>
                    <p class="card-text mb-1">
                        <strong>@Html.DisplayNameFor(Model => Model.Location)</strong>
                        @item.Location
                    </p>
                    <p class="card-text">
                        <strong>Capacity:</strong>
                        @item.Capacity
                    </p>
                </div>

                @* Card Buttons for Edit|Details|Delete *@
                <div class="card-footer bg-transparent border-top-0">
                    <a asp-action="Edit" asp-route-id="@item.VenueId" class="btn btn-sm btn-outline-primary me-1">Edit</a>
                    <a asp-action="Details" asp-route-id="@item.VenueId" class="btn btn-sm btn-outline-secondary me-1">Details</a>
                    <a asp-action="Delete" asp-route-id="@item.VenueId" class="btn btn-sm btn-outline-danger">Delete</a>
                </div>
            </div>
        </div>
    }
</div>